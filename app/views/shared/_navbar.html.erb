  <nav id="main-navbar" class="navbar navbar-expand-md fixed-top main-menu p-0">
    <div class="container">
      <div class="collapse navbar-collapse" id="mainNav">
        <ul class="navbar-nav mr-auto"></ul>
        <ul class="navbar-nav ml-auto">
          <li class="nav-item">
            <% if current_page?(home_index_path) %>
              <a class="nav-link active" href="#landing-top">Home</a>
            <% else %>
              <%= link_to 'Home', home_index_path, class: 'nav-link' %>
            <% end %>
          </li>
          <% if current_page?(home_index_path) %>
            <li class="nav-item">
              <a class="nav-link" href="#landing-projects">Projects</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#landing-footer">Contact</a>
            </li>
          <% end %>
          <li class="nav-item">
            <% if current_page?(categories_index_path) %>
              <a class="nav-link active" href="#categories-top">Blog</a>
            <% else %>
              <%= link_to 'Blog', categories_index_path, class: 'nav-link' %>
            <% end %>
          </li>
        </ul>
      </div>
    </div>
  </nav>